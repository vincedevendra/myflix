- @categories.each do |category|
  - if category.videos.any?
    %article.video_category
      %header
        %h3= link_to category.title, category
      .videos.row
        - category.recent_videos.each do |video|
          .video.col-sm-2
            = link_to video do
              = image_tag video.small_cover_url

